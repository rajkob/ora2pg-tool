# ── Secrets ───────────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
_run.env
_run.conf

# ── Maintainer-only docs (never publish) ─────────────────────────────────────
MAINTAINER.md

# ── Oracle Instant Client ZIPs (large binaries, download separately) ──────────
vendor/*.zip

# ── Migration output ──────────────────────────────────────────────────────────
schema/
data/
logs/
*.log

# ── ora2pg runtime configs (generated by migrate scripts) ─────────────────────
ora2pg.conf
ora2pg_*.conf

# ── Local test environment (not for distribution) ─────────────────────────────
test/

# ── OS / editor ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.vscode/
