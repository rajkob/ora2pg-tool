# ── Secrets ───────────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
_run.env
_run.conf

# ── Oracle Instant Client ZIPs (large binaries, download separately) ──────────
vendor/*.zip

# ── Migration output ──────────────────────────────────────────────────────────
schema/
data/
logs/
*.log

# ── ora2pg runtime configs (generated by migrate scripts) ─────────────────────
ora2pg.conf
ora2pg_*.conf

# ── OS / editor ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.vscode/
